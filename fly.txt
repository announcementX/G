local player = game.Players.LocalPlayer
local gui = Instance.new("ScreenGui", player.PlayerGui)
gui.ResetOnSpawn = false

local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

-- 主框架
local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 300, 0, 190)
frame.Position = UDim2.new(0.35,0,0.3,0)
frame.BackgroundColor3 = Color3.fromRGB(10,10,15)
frame.Active = true
frame.Draggable = true
Instance.new("UICorner", frame).CornerRadius = UDim.new(0,16)

-- 星空（底层）
local starLayer = Instance.new("Frame", frame)
starLayer.Size = UDim2.new(1,0,1,0)
starLayer.BackgroundTransparency = 1
starLayer.ZIndex = 0

for i = 1,35 do
	local star = Instance.new("Frame", starLayer)
	star.Size = UDim2.new(0,2,0,2)
	star.Position = UDim2.new(math.random(),0,math.random(),0)
	star.BackgroundColor3 = Color3.fromRGB(200,200,255)
	star.BorderSizePixel = 0
	star.ZIndex = 0
	Instance.new("UICorner", star).CornerRadius = UDim.new(1,0)

	task.spawn(function()
		while star.Parent do
			TweenService:Create(star,TweenInfo.new(math.random(4,8)),{
				Position = UDim2.new(math.random(),0,math.random(),0)
			}):Play()
			wait(math.random(4,8))
		end
	end)
end

-- UI容器（关键！）
local ui = Instance.new("Frame", frame)
ui.Size = UDim2.new(1,0,1,0)
ui.BackgroundTransparency = 1
ui.ZIndex = 2

-- 标题
local title = Instance.new("TextLabel", ui)
title.Size = UDim2.new(1,0,0,30)
title.BackgroundTransparency = 1
title.Text = "✦ FLIGHT"
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.TextColor3 = Color3.fromRGB(200,220,255)
title.ZIndex = 2

-- 速度显示
local speedText = Instance.new("TextLabel", ui)
speedText.Position = UDim2.new(0,0,0,35)
speedText.Size = UDim2.new(1,0,0,40)
speedText.BackgroundTransparency = 1
speedText.Font = Enum.Font.GothamBlack
speedText.TextSize = 26
speedText.Text = "SPEED 1"
speedText.TextColor3 = Color3.fromRGB(0,170,255)
speedText.ZIndex = 2

-- 按钮容器
local btnHolder = Instance.new("Frame", ui)
btnHolder.Position = UDim2.new(0,0,0,80)
btnHolder.Size = UDim2.new(1,0,0,100)
btnHolder.BackgroundTransparency = 1
btnHolder.ZIndex = 2

local grid = Instance.new("UIGridLayout", btnHolder)
grid.CellSize = UDim2.new(0,65,0,35)
grid.CellPadding = UDim2.new(0,10,0,10)

-- 按钮函数
local function btn(text)
	local b = Instance.new("TextButton", btnHolder)
	b.Text = text
	b.Font = Enum.Font.GothamBold
	b.TextSize = 16
	b.TextColor3 = Color3.new(1,1,1)
	b.BackgroundColor3 = Color3.fromRGB(30,30,40)
	b.ZIndex = 2
	Instance.new("UICorner",b).CornerRadius = UDim.new(0,10)

	return b
end

local up = btn("⬆")
local down = btn("⬇")
local plus = btn("＋")
local minus = btn("－")
local fly = btn("✈")

-- 小球
local ball = Instance.new("TextButton", gui)
ball.Size = UDim2.new(0,55,0,55)
ball.Position = UDim2.new(0.1,0,0.5,0)
ball.Text = "✦"
ball.Visible = false
ball.BackgroundColor3 = Color3.fromRGB(20,20,30)
ball.Active = true
ball.Draggable = true
Instance.new("UICorner",ball).CornerRadius = UDim.new(1,0)

-- 飞行变量
local flying = false
local speed = 1
local vertical = 0
local bv,bg

local function startFly()
	flying = true
	local root = player.Character:WaitForChild("HumanoidRootPart")

	bv = Instance.new("BodyVelocity", root)
	bv.MaxForce = Vector3.new(9e9,9e9,9e9)

	bg = Instance.new("BodyGyro", root)
	bg.MaxTorque = Vector3.new(9e9,9e9,9e9)

	RunService:BindToRenderStep("fly",0,function()
		if not flying then return end

		local cam = workspace.CurrentCamera

		-- 移动（完全跟镜头）
		local move = cam.CFrame.LookVector * speed * 5
		move += Vector3.new(0, vertical*speed*3, 0)

		bv.Velocity = move

		-- 🔥 核心：强制朝向（你要的效果）
		bg.CFrame = cam.CFrame
	end)
end

local function stopFly()
	flying = false
	RunService:UnbindFromRenderStep("fly")
	if bv then bv:Destroy() end
	if bg then bg:Destroy() end
end

-- 控制
fly.MouseButton1Click:Connect(function()
	if flying then stopFly() else startFly() end
end)

plus.MouseButton1Click:Connect(function()
	speed += 1
	speedText.Text = "SPEED "..speed
end)

minus.MouseButton1Click:Connect(function()
	if speed > 1 then speed -= 1 end
	speedText.Text = "SPEED "..speed
end)

up.MouseButton1Down:Connect(function() vertical = 1 end)
up.MouseButton1Up:Connect(function() vertical = 0 end)

down.MouseButton1Down:Connect(function() vertical = -1 end)
down.MouseButton1Up:Connect(function() vertical = 0 end)

-- 收起
title.InputBegan:Connect(function()
	frame.Visible = false
	ball.Visible = true
end)

ball.MouseButton1Click:Connect(function()
	frame.Visible = true
	ball.Visible = false
end)